# EMBEDDED SERVER CONFIGURATION
server.servlet.context-path=/stomp-chat

# ERRORS
error.whitelabel.enabled=true

# Logging config
logging.level.org.springframework=DEBUG
logging.level.org.hibernate=DEBUG
spring.jpa.show-sql=true
